#include "holberton.h"

/**
 * _strlen - calculates the length of a string
 *
 * @s: string
 * Return: lenght of string.
 */

int _strlen(char *s)
{
int a;
for (a = 0; *(s + a) != '\0'; a++)
{
}
return (a);
}
